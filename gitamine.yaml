gitamine:

  pre-commit:
    #test: ~
    shellcheck: ~
    composer: ~
    yaml-lint: ~
    phpcs:
      interactive: false
      standard: PSR2
      enabled: true
      cbf: true
    phpmd: ~
    phpunit:
      min-coverage: 100
      #TODO configuration-file: phpunit.unit.xml

  pre-push:
    codeception:
      enabled: false
    phpunit:
      enabled: true
      min-coverage: 100
