gitamine:
  plugins:
    #test: ~
    phplint: ~
    yaml-lint: ~
    phpcs:
      enabled: true
    phpunit:
      bin: bin/phpunit
      enabled: true
      min-coverage: 100
    #symfony: ~

# plugins contains an array of assoc arrays, 'enabled' is the only option that is used in gitamine, the other options
# are sent to the plugin (don't know how)
